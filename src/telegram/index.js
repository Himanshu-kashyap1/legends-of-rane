/**
 * Telegram Transport & Presentation Layer
 *
 * - bot.js         : Telegraf initialization & lifecycle
 * - commands/      : Text commands (/start, /profile, /explore, etc.)
 * - handlers/      : Update handlers and dispatchers
 * - middlewares/   : UserLoader, ActionLock (race guard), OwnershipGuard
 * - buttons/       : Inline callback query router and action dispatchers
 * - cards/         : SVG to PNG high-resolution card generator
 * - views/         : Message formatters and keyboard layouts
 */
export default {};
